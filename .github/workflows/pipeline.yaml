name: CD-HealthPanel-Backend

on:
  workflow_dispatch:
  #push:
  #  branches:
  #    - main

jobs:
  Deploy-ssh:

    runs-on: ubuntu-latest

    steps:
      - name: Deploy-ssh
        uses: invi5H/ssh-action@v1
        with:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      - run: |
          cd db1-projects/;
          touch teste_pipe.txt
